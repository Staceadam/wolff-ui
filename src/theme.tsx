import { createTheme as restyleCreateTheme, ThemeProvider as RestyleThemeProvider } from '@shopify/restyle'

export const createTheme = restyleCreateTheme

export const ThemeProvider = RestyleThemeProvider
