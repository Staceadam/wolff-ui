import type { ShadowStyleIOS } from 'react-native'

export const shadowIOSProps = {
  shadowColor: true,
  shadowOffset: true,
  shadowOpacity: true,
  shadowRadius: tru
}

export interface ShadowStylesIOSProps extends ShadowStyleIOS {}
