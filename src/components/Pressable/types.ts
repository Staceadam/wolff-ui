import type { PressableProps as RNPressableProps } from 'react-native'
import type { ViewStyleProps } from '../../config'

export interface PressableProps extends RNPressableProps, ViewStyleProps {
}
